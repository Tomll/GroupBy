<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 矩形的圆角弧度 -->
    <corners android:radius="10dp" />

    <!-- 描边 -->
    <!--    <stroke
            android:width="1dp"
            android:color="@android:color/darker_gray" />-->

    <!-- 矩形的填充色 -->
    <!--    <solid android:color="@android:color/darker_gray" />-->


    <gradient
        android:angle="90"
        android:centerX="0.5"
        android:centerY="0.5"
        android:endColor="#fffff3c7"
        android:startColor="#fffffdf5"
        android:type="linear"
        android:useLevel="false" />
    <!--
        <gradient android:type=["linear" | "radial" | "sweep"] //共有3中渐变类型，线性渐变（默认）/放射渐变/扫描式渐变；
        android:angle="90" //渐变角度，必须为45的倍数，0为从左到右，180为从右到左，90为从下到上，270为从上到下；
        android:centerX="0.5" //渐变中心X的相当位置，范围为0～1；
        android:centerY="0.5" //渐变中心Y的相当位置，范围为0～1；
        android:startColor="#24e9f2" //渐变开始点的颜色；
        android:centerColor="#2564ef" //渐变中间点的颜色，在开始与结束点之间；
        android:endColor="#25f1ef" //渐变结束点的颜色；
        android:gradientRadius="5dp" //渐变的半径，只有当渐变类型为radial时才能使用；
        android:useLevel="false" /> //使用LevelListDrawable时就要设置为true。设为false时才有渐变效果。
    -->

    <!-- 矩形的边框的宽度,每段虚线的长度，和两段虚线之间的颜色和颜色 -->
    <!--<stroke
        android:width="1dp"
        android:color="#4eb621"
        android:dashGap="4dp"
        android:dashWidth="8dp" />-->


</shape>